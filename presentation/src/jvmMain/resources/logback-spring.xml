<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- Console appender for all profiles -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- LOG_PATH property set programmatically via System.setProperty before Spring initialization -->
    <property name="LOG_PATH" value="${logging.file.path}"/>
    
    <!-- Development profile - logs to programmatically determined directory -->
    <springProfile name="dev,default,!prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/dev.log</file>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/dev.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                    <maxFileSize>50MB</maxFileSize>
                </timeBasedFileNamingAndTriggeringPolicy>
                <maxHistory>7</maxHistory>
                <totalSizeCap>500MB</totalSizeCap>
            </rollingPolicy>
        </appender>
        
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        
        <!-- Debug level for our components in dev -->
        <logger name="com.gromozeka.bot" level="DEBUG"/>
    </springProfile>
    
    <!-- Production profile - logs to programmatically determined directory -->
    <springProfile name="prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/gromozeka.log</file>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/gromozeka.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                    <maxFileSize>100MB</maxFileSize>
                </timeBasedFileNamingAndTriggeringPolicy>
                <maxHistory>30</maxHistory>
                <totalSizeCap>3GB</totalSizeCap>
            </rollingPolicy>
        </appender>
        
        <root level="INFO">
            <appender-ref ref="FILE"/>
        </root>
        
        <!-- Production logging levels -->
        <logger name="com.gromozeka.bot" level="INFO"/>
        <logger name="com.gromozeka.bot.services.StreamLogger" level="WARN"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.apache" level="WARN"/>
    </springProfile>
    
    <!-- Specific logger configurations -->
    <logger name="org.springframework.boot" level="WARN"/>
    <logger name="org.springframework.web" level="WARN"/>
    <logger name="org.springframework.ai" level="DEBUG"/>
    <!-- Disable noisy ChatResponseMetadata logs (writes 4 lines per streaming chunk) -->
    <logger name="org.springframework.ai.chat.metadata.ChatResponseMetadata" level="WARN"/>
    <logger name="org.springframework.ai.claudecode" level="DEBUG"/>
    <logger name="org.springframework.ai.claudecode.api.ClaudeCodeApi" level="DEBUG"/>
    <logger name="org.springframework.ai.claudecode.converter" level="DEBUG"/>
    <logger name="com.gromozeka.bot.model.SessionSpringAI" level="DEBUG"/>
    <logger name="com.fasterxml.jackson" level="WARN"/>
    <logger name="io.ktor" level="INFO"/>
    <logger name="org.apache.batik" level="WARN"/>
    
</configuration>
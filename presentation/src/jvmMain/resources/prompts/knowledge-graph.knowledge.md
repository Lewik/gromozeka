# Knowledge Graph And Memory

Legacy graph-first memory instructions are obsolete.

Use `memory.knowledge.md` for the current runtime memory contract.

Do not call memory tools unless they are actually present in the tool list.
Runtime recall may inject relevant memory automatically before the answer.

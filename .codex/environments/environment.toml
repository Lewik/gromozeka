# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "gromozeka-dev"

[setup]
script = ""

[[actions]]
name = "Run"
icon = "run"
command = '''
bash -c 'set -euo pipefail
ROOT_DIR="$(pwd)"
POSTGRES_HOST="${GROMOZEKA_POSTGRES_HOST:-127.0.0.1}"
POSTGRES_PORT="${GROMOZEKA_POSTGRES_PORT:-5432}"
RABBIT_HOST="${GROMOZEKA_RABBIT_HOST:-127.0.0.1}"
RABBIT_PORT="${GROMOZEKA_RABBIT_PORT:-5672}"
if ! nc -z "$POSTGRES_HOST" "$POSTGRES_PORT" >/dev/null 2>&1; then
  echo "Postgres is not available at $POSTGRES_HOST:$POSTGRES_PORT." >&2
  echo "Start it with the \"Start Runtime Infra\" action first." >&2
  exit 1
fi
if ! nc -z "$RABBIT_HOST" "$RABBIT_PORT" >/dev/null 2>&1; then
  echo "RabbitMQ is not available at $RABBIT_HOST:$RABBIT_PORT." >&2
  echo "Start it with the \"Start Runtime Infra\" action first." >&2
  exit 1
fi
export GROMOZEKA_MODE="${GROMOZEKA_MODE:-dev}"
export GROMOZEKA_HOME="${GROMOZEKA_HOME:-$ROOT_DIR/dev-data/client/.gromozeka}"
export JAVA_HOME="${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}"
export PATH="$JAVA_HOME/bin:$PATH"
mkdir -p "$ROOT_DIR/logs" "$GROMOZEKA_HOME"
exec ./gradlew :presentation:run -q'
'''

[[actions]]
name = "Start Runtime Infra"
icon = "tool"
command = '''
bash -c 'set -euo pipefail
ROOT_DIR="$(pwd)"
export GROMOZEKA_HOME="${GROMOZEKA_HOME:-$ROOT_DIR/dev-data/client/.gromozeka}"
docker compose -f "$ROOT_DIR/server/src/main/resources/docker-compose.yml" up -d postgres rabbitmq'
'''

[[actions]]
name = "Stop Runtime Infra"
icon = "tool"
command = '''
bash -c 'set -euo pipefail
ROOT_DIR="$(pwd)"
docker compose -f "$ROOT_DIR/server/src/main/resources/docker-compose.yml" stop postgres rabbitmq'
'''

[[actions]]
name = "Start Tailscale Web"
icon = "tool"
command = '''
bash -c 'set -euo pipefail
PORT="${GROMOZEKA_REMOTE_PORT:-8765}"
tailscale serve --bg "http://127.0.0.1:${PORT}"
tailscale serve status'
'''

[[actions]]
name = "Stop Tailscale Web"
icon = "tool"
command = '''
bash -c 'set -euo pipefail
tailscale serve reset'
'''

[[actions]]
name = "Build"
icon = "tool"
command = "bash -c 'set -euo pipefail; export JAVA_HOME=\"${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}\"; export PATH=\"$JAVA_HOME/bin:$PATH\"; ./gradlew :presentation:build -q'"

[[actions]]
name = "Server Tests"
icon = "test"
command = "bash -c 'set -euo pipefail; export JAVA_HOME=\"${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}\"; export PATH=\"$JAVA_HOME/bin:$PATH\"; ./gradlew :server:test -q'"

[[actions]]
name = "Memory Unit Tests"
icon = "test"
command = "bash -c 'set -euo pipefail; export JAVA_HOME=\"${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}\"; export PATH=\"$JAVA_HOME/bin:$PATH\"; ./gradlew :application:jvmTest --tests '\\''com.gromozeka.application.service.memory.MemoryMaintenancePipelineTest'\\'' -q'"

[[actions]]
name = "Memory Real E2E"
icon = "test"
command = "bash -c 'set -euo pipefail; export JAVA_HOME=\"${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}\"; export PATH=\"$JAVA_HOME/bin:$PATH\"; ./gradlew :server:test --tests '\\''com.gromozeka.server.MemoryRealModelE2eTest'\\'' -Dgromozeka.memory.e2e=true -Dgromozeka.llm.cassette.mode=replay-only -q'"

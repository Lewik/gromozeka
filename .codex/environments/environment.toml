# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "gromozeka-dev"

[setup]
script = ""

[[actions]]
name = "Run"
icon = "run"
command = '''
bash -c 'set -euo pipefail
ROOT_DIR="$(pwd)"
MONGO_HOST="${GROMOZEKA_MONGO_HOST:-127.0.0.1}"
MONGO_PORT="${GROMOZEKA_MONGO_PORT:-27017}"
if ! nc -z "$MONGO_HOST" "$MONGO_PORT" >/dev/null 2>&1; then
  echo "MongoDB is not available at $MONGO_HOST:$MONGO_PORT." >&2
  echo "Start it with the \"Start Mongo\" action first." >&2
  exit 1
fi
export GROMOZEKA_MODE="${GROMOZEKA_MODE:-dev}"
export GROMOZEKA_HOME="${GROMOZEKA_HOME:-$ROOT_DIR/dev-data/client/.gromozeka}"
export JAVA_HOME="${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}"
export PATH="$JAVA_HOME/bin:$PATH"
mkdir -p "$ROOT_DIR/logs" "$GROMOZEKA_HOME"
exec ./gradlew :presentation:run -q'
'''

[[actions]]
name = "Start Mongo"
icon = "tool"
command = '''
bash -c 'set -euo pipefail
ROOT_DIR="$(pwd)"
export GROMOZEKA_HOME="${GROMOZEKA_HOME:-$ROOT_DIR/dev-data/client/.gromozeka}"
docker compose -f "$ROOT_DIR/presentation/src/jvmMain/resources/docker-compose.yml" up -d mongodb'
'''

[[actions]]
name = "Stop Mongo"
icon = "tool"
command = '''
bash -c 'set -euo pipefail
ROOT_DIR="$(pwd)"
docker compose -f "$ROOT_DIR/presentation/src/jvmMain/resources/docker-compose.yml" stop mongodb'
'''

[[actions]]
name = "Build"
icon = "tool"
command = "bash -c 'set -euo pipefail; export JAVA_HOME=\"${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}\"; export PATH=\"$JAVA_HOME/bin:$PATH\"; ./gradlew :presentation:build -q'"

[[actions]]
name = "App Context Test"
icon = "test"
command = "bash -c 'set -euo pipefail; export JAVA_HOME=\"${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}\"; export PATH=\"$JAVA_HOME/bin:$PATH\"; ./gradlew :presentation:build :presentation:jvmTest --tests ApplicationContextTest -q'"

[[actions]]
name = "Memory Unit Tests"
icon = "test"
command = "bash -c 'set -euo pipefail; export JAVA_HOME=\"${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}\"; export PATH=\"$JAVA_HOME/bin:$PATH\"; ./gradlew :application:jvmTest --tests '\\''com.gromozeka.application.service.memory.MemoryMaintenancePipelineTest'\\'' -q'"

[[actions]]
name = "Memory Real E2E"
icon = "test"
command = "bash -c 'set -euo pipefail; export JAVA_HOME=\"${GROMOZEKA_JAVA_HOME:-$(/usr/libexec/java_home -v 21)}\"; export PATH=\"$JAVA_HOME/bin:$PATH\"; ./gradlew :presentation:jvmTest --tests '\\''com.gromozeka.presentation.MemoryRealModelE2eTest'\\'' -Dgromozeka.memory.e2e=true -Dgromozeka.llm.cassette.mode=replay-only -q'"
